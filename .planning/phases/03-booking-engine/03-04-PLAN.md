---
phase: 03-booking-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-03
files_modified:
  - client/src/features/availability/components/DaySidePanel.tsx
  - client/src/features/availability/components/MonthGrid.tsx
  - client/src/features/availability/pages/UserCalendarPage.tsx
  - client/src/features/availability/pages/MyCalendarPage.tsx
  - client/src/features/booking/api/bookingApi.ts
  - client/src/features/booking/components/BookingConfirmDialog.tsx
  - client/src/features/booking/components/BookingTimeline.tsx
autonomous: true
requirements:
  - CAL-03
  - BOOK-01

must_haves:
  truths:
    - "Booker views another user's calendar (/users/{id}), clicks a day, and sees a timeline of free/booked slots in the side panel"
    - "Booker selects duration (30 min, 1 hr, 2 hr, Custom) and start time from free slots only"
    - "'Request Booking' button is disabled until both duration and start time are selected"
    - "Clicking 'Request Booking' opens a confirmation Dialog with 'Confirm your booking' title, slot details, and owner name"
    - "Confirming the dialog fires POST /api/v1/bookings → success toast 'Request sent — awaiting approval.'"
    - "Owner views their own calendar, clicks a day with bookings: side panel shows Pending blocks with Accept/Decline buttons and Confirmed blocks with two-step Cancel"
    - "Month grid shows color-coded booking blocks — Pending: amber, Confirmed: emerald — on owner's own calendar"
    - "Month grid shows booked slots as 'Unavailable' zinc blocks on read-only /users/{id} calendar"
  artifacts:
    - path: "client/src/features/booking/components/BookingTimeline.tsx"
      provides: "Timeline UI of free/booked slots in side panel"
      contains: "free blocks, booked blocks, duration picker, start time picker"
    - path: "client/src/features/booking/components/BookingConfirmDialog.tsx"
      provides: "Confirmation Dialog before submitting booking request"
      contains: "Dialog, 'Confirm your booking', 'Request Booking', 'Keep this time'"
    - path: "client/src/features/availability/components/DaySidePanel.tsx"
      provides: "Side panel with booking section (booker + owner views)"
      contains: "BookingTimeline, Accept Booking, Decline Booking, Cancel Booking"
    - path: "client/src/features/availability/components/MonthGrid.tsx"
      provides: "Month grid with booking blocks per status"
      contains: "bg-amber-100, bg-emerald-100"
  key_links:
    - from: "UserCalendarPage.tsx"
      to: "GET /api/v1/availability/slots?ownerId=&date="
      via: "bookingApi.getAvailableSlots called when side panel opens"
      pattern: "getAvailableSlots"
    - from: "DaySidePanel.tsx (booker mode)"
      to: "BookingConfirmDialog"
      via: "Request Booking button → dialog open state"
      pattern: "BookingConfirmDialog"
    - from: "MonthGrid.tsx"
      to: "BookingDto[] bookings prop"
      via: "renders booking blocks alongside availability blocks"
      pattern: "bookings"
---

<objective>
Extend DaySidePanel, MonthGrid, UserCalendarPage, and MyCalendarPage to integrate the booking experience into the existing calendar UI. Add BookingTimeline (free/booked slot view) and BookingConfirmDialog for the booker flow, and Accept/Decline/Cancel actions in the owner's side panel.

Purpose: Completes the end-to-end booking flow in the calendar UI — bookers request from /users/{id}, owners approve from their own calendar. This is the last plan making the full booking lifecycle accessible from the calendar.
Output: BookingTimeline.tsx, BookingConfirmDialog.tsx, extended DaySidePanel.tsx, extended MonthGrid.tsx, UserCalendarPage.tsx with slot fetching.
</objective>

<execution_context>
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-booking-engine/03-CONTEXT.md
@.planning/phases/03-booking-engine/03-UI-SPEC.md
@client/src/features/availability/components/DaySidePanel.tsx
@client/src/features/availability/components/MonthGrid.tsx
@client/src/features/availability/pages/UserCalendarPage.tsx
@client/src/features/availability/pages/MyCalendarPage.tsx
@client/src/features/booking/api/bookingApi.ts
@client/src/features/booking/hooks/useBookings.ts
@client/src/features/auth/useAuth.ts
</context>

<interfaces>
<!-- Key types and contracts extracted from existing code -->

From client/src/features/availability/components/DaySidePanel.tsx (existing props interface):
  DaySidePanelProps: {
    date: string | null;
    ranges: TimeRangeDto[];
    onRangesChange: (ranges: TimeRangeDto[]) => void;
    onClose: () => void;
    onSave: () => void;
    onResetToTemplate?: () => void;
    isOverride?: boolean;
    isSaving?: boolean;
    readOnly?: boolean;
  }
  Note: Must extend props rather than replace — existing MyCalendarPage still uses current interface.
  New props to add: bookings?: BookingDto[], ownerId?: string (for slots fetch in booker mode), onDayBookingsRefetch?: () => void

From client/src/features/availability/components/MonthGrid.tsx (existing props):
  MonthGridProps: { year, month, dayData?, onDayClick?, readOnly? }
  New prop to add: bookings?: BookingDto[] (list of bookings to render as colored blocks)

From client/src/features/booking/api/bookingApi.ts (already created in Plan 03-03):
  - bookingApi.getAvailableSlots(ownerId: string, date: string) — needs adding
  - bookingApi.createBooking(data: CreateBookingRequest) — already exists
  Returns: { availableSlots: TimeRangeDto[], bookedSlots: BookingSlotDto[] }
  BookingSlotDto: { startTime: string, endTime: string, status: string }

From UI-SPEC.md §1 timeline blocks:
  Free block: "bg-white border border-dashed border-border" + label "Available" + time range
  Booked block (per status color): bg-amber-100/emerald-100/red-100/zinc-100 per status table

From UI-SPEC.md §3 Dialog spec:
  Title: "Confirm your booking"
  Body: "Book {StartTime}–{EndTime} with {OwnerName} on {Day, Month D, YYYY}?"
  Sub-text: "Your request will be sent for approval."
  CTA: "Request Booking" (primary, full-width)
  Cancel: "Keep this time" (outline/ghost, full-width)
  Loading: CTA shows spinner + "Sending…", both disabled

From client/src/features/auth/useAuth.ts:
  useAuth() returns { user: { id, email } | null, ... }
  Use user.id to detect own calendar vs viewed calendar (own: userId param matches user.id)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BookingTimeline, BookingConfirmDialog, and bookingApi slots endpoint</name>
  <read_first>
    - client/src/features/booking/api/bookingApi.ts (add getAvailableSlots method)
    - client/src/features/booking/hooks/useBookings.ts (add useAvailableSlots hook)
    - client/src/features/booking/components/BookingCard.tsx (status color helper pattern)
    - .planning/phases/03-booking-engine/03-UI-SPEC.md §1 timeline spec, §3 dialog spec
    - client/src/components/ui/dialog.tsx (just installed — understand Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter export names)
  </read_first>
  <files>
    client/src/features/booking/api/bookingApi.ts,
    client/src/features/booking/hooks/useBookings.ts,
    client/src/features/booking/components/BookingTimeline.tsx,
    client/src/features/booking/components/BookingConfirmDialog.tsx
  </files>
  <action>
    Update bookingApi.ts: add BookingSlotDto interface: { startTime: string; endTime: string; status: string; }. Add SlotsResponse interface: { availableSlots: { start: string; end: string; }[]; bookedSlots: BookingSlotDto[]; }. Add method to bookingApi: getAvailableSlots: (ownerId: string, date: string) => apiClient.get<SlotsResponse>('/availability/slots', { params: { ownerId, date } })

    Update useBookings.ts: export useAvailableSlots(ownerId: string | undefined, date: string | null): useQuery({ queryKey: ['slots', ownerId, date], queryFn: () => bookingApi.getAvailableSlots(ownerId!, date!).then(r => r.data), enabled: !!ownerId && !!date })

    Create client/src/features/booking/components/BookingTimeline.tsx. Props:
    - availableSlots: Array<{ start: string; end: string }>
    - bookedSlots: Array<{ startTime: string; endTime: string; status: string }>
    - selectedDuration: number | null (minutes: 30, 60, 120, or custom)
    - selectedStartTime: string | null (HH:mm)
    - onDurationChange: (minutes: number) => void
    - onStartTimeChange: (time: string) => void
    - isLoading: boolean

    Render sections:
    1. Duration picker: button group with options 30 (label "30 min"), 60 (label "1 hr"), 120 (label "2 hr"), 0 (label "Custom"). When "Custom" selected, show an <input type="number" placeholder="e.g. 45" min="15" max="480"> for minutes. Selected button: bg-primary text-primary-foreground. Unselected: border text-foreground hover:bg-muted.

    2. Timeline section (rendered after duration selected or always): two types of blocks stacked vertically:
       - Available (free) slot: div className="mt-1 rounded p-2 bg-white border border-dashed border-border cursor-pointer". Label: <p className="text-xs font-semibold">Available</p>, time range below. Clicking a free block sets selectedStartTime to block.start. Highlight selected block with ring-2 ring-primary.
       - Booked slot: div with status classes (Pending: bg-amber-100 border border-amber-300 text-amber-800; Confirmed: bg-emerald-100 border border-emerald-400 text-emerald-800). Shows status name only ("Pending", "Confirmed") — no booker name (privacy). Not clickable.

    3. Free slot generation: given availableSlots (windows from owner's availability) and bookedSlots (Pending+Confirmed), compute free time intervals with 15-min start increments. A start time is valid if start + selectedDuration fits within the free gap. Show valid start times as clickable free blocks. If selectedDuration is null, show all free windows without start time granularity (just the raw free intervals).

    4. isLoading: show a skeleton/text "Loading availability..." instead of timeline.

    Empty state: "No available time on this day."

    Create client/src/features/booking/components/BookingConfirmDialog.tsx. Props:
    - open: boolean
    - onOpenChange: (open: boolean) => void
    - ownerName: string
    - date: string (yyyy-MM-dd)
    - startTime: string (HH:mm)
    - endTime: string (HH:mm) — computed as start + duration
    - onConfirm: () => void
    - isSubmitting: boolean

    Use Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter from '@/components/ui/dialog'. Format date with date-fns format(new Date(date + 'T00:00:00'), 'EEEE, MMMM d, yyyy').

    Render:
    - DialogTitle: "Confirm your booking"
    - Body: "Book {startTime}–{endTime} with {ownerName} on {formattedDate}?" — bold startTime, endTime, ownerName using <strong>
    - Sub-text: p className="text-sm text-muted-foreground" text "Your request will be sent for approval."
    - Buttons stacked full-width: first "Request Booking" (Button variant="default" className="w-full" disabled={isSubmitting}), second "Keep this time" (Button variant="outline" className="w-full" onClick={() => onOpenChange(false)} disabled={isSubmitting}). When isSubmitting: show spinner span + "Sending…" in the CTA button.
  </action>
  <verify>
    <automated>cd client; npx tsc --noEmit 2>&1 | Select-String "error" | Select-Object -First 20</automated>
  </verify>
  <acceptance_criteria>
    - client/src/features/booking/api/bookingApi.ts contains "getAvailableSlots" function
    - client/src/features/booking/hooks/useBookings.ts exports useAvailableSlots
    - client/src/features/booking/components/BookingTimeline.tsx exists with duration picker (30, 60, 120, custom) and timeline sections
    - BookingTimeline.tsx contains "Custom" label and input type="number" for custom duration
    - client/src/features/booking/components/BookingConfirmDialog.tsx exists and contains "Confirm your booking" and "Request Booking" and "Keep this time"
    - BookingConfirmDialog.tsx contains "Your request will be sent for approval."
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>BookingTimeline renders free/booked slots with duration + start time selection. BookingConfirmDialog is a complete confirmation modal with loading state and correct copy from UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Extend DaySidePanel and MonthGrid with booking integration</name>
  <read_first>
    - client/src/features/availability/components/DaySidePanel.tsx (full file — extend, do not replace)
    - client/src/features/availability/components/MonthGrid.tsx (full file — extend, do not replace)
    - client/src/features/availability/pages/UserCalendarPage.tsx (full file — extend with booking state)
    - client/src/features/availability/pages/MyCalendarPage.tsx (understand how it calls DaySidePanel — ensure backward compat)
    - client/src/features/booking/components/BookingTimeline.tsx (just created — understand props)
    - client/src/features/booking/components/BookingConfirmDialog.tsx (just created)
    - client/src/features/auth/useAuth.ts (useAuth hook — for user.id)
  </read_first>
  <files>
    client/src/features/availability/components/DaySidePanel.tsx,
    client/src/features/availability/components/MonthGrid.tsx,
    client/src/features/availability/pages/UserCalendarPage.tsx
  </files>
  <action>
    Update DaySidePanel.tsx. The goal is extension, not replacement — all existing props remain unchanged and MyCalendarPage continues to work with the same interface.

    New optional props to add to DaySidePanelProps:
    - mode?: 'owner' | 'booker' | 'read-only' (default 'read-only' for backward compat when readOnly=true is passed)
    - bookings?: BookingDto[] (bookings for the selected date — used in both modes)
    - ownerId?: string (calendar owner's userId — used by booker mode to fetch slots)
    - ownerEmail?: string (owner's email — used in confirmation dialog)
    - onBookingAction?: () => void (callback to refetch bookings after accept/decline/cancel)

    In the body section, after the existing DayTimeRangeEditor (which only renders when !readOnly || mode==='owner'):

    If mode === 'booker' (viewing someone else's calendar):
    - Render BookingTimeline below the existing availability display
    - Fetch slots using useAvailableSlots(ownerId, date) inside DaySidePanel (or accept as props — prefer accepting as props to keep component pure; fetch in UserCalendarPage and pass down)
    - Manage local state: selectedDuration (number | null), selectedStartTime (string | null), dialogOpen (boolean), isSubmittingBooking (boolean)
    - Render "Request Booking" button (Button variant="default" className="w-full mt-3" disabled={!selectedDuration || !selectedStartTime}): on click, set dialogOpen=true
    - Render BookingConfirmDialog with open=dialogOpen, onConfirm async handler that: calls useCreateBooking mutation, on success closes panel + shows toast, on error shows error toast
    - Compute endTime from selectedStartTime + selectedDuration (add minutes using date-fns addMinutes on a dummy Date then format HH:mm)

    If mode === 'owner' (own calendar with bookings present):
    - After the availability ranges section, render a section "Bookings on this day" if bookings.length > 0
    - For each booking in bookings for this date, render a card block with status styling from UI-SPEC
    - Pending booking block: show bookerEmail + startTime–endTime + Status badge (amber) + "Accept Booking" button (Button variant="default" min-h-[44px]) + "Decline Booking" button (Button variant="outline" border-destructive text-destructive min-h-[44px]). Clicking Accept → useAcceptBooking(booking.id), clicking Decline → useDeclineBooking(booking.id). Both call onBookingAction?.() on success.
    - Confirmed booking block: show bookerEmail + startTime–endTime + Status badge (emerald). Inline two-step cancel: useState<string|null>(null) as cancelConfirmId. "Cancel Booking" button (destructive-outline); if cancelConfirmId===booking.id, show "Yes, cancel it" (destructive) + "Keep it" (ghost); "Yes, cancel it" calls useCancelBooking(booking.id) + onBookingAction?.().
    - Declined/Cancelled booking block: read-only, show email + time + badge only.

    Update MonthGrid.tsx. Add optional prop: bookings?: BookingDto[]. Inside the day cell render loop, after the existing availability range blocks, add booking block rendering:

    Filter bookings where booking.date === dateStr. For each matching booking:
    - If !readOnly (own calendar): render status block using classes from UI-SPEC: Pending → "bg-amber-100 border border-amber-300 text-amber-800", Confirmed → "bg-emerald-100 border border-emerald-400 text-emerald-800". Label: status name ("Pending" or "Confirmed"). Time: booking.startTime + "–" + booking.endTime in text-[9px]. Add title attribute: "Pending: {startTime}–{endTime} with {bookerEmail}" or "Confirmed: {startTime}–{endTime} with {bookerEmail}" (per UI-SPEC accessibility note).
    - If readOnly (user's calendar seen by others): render zinc block "bg-zinc-100 border border-zinc-300 text-zinc-500" with label "Unavailable". No booker info exposed.

    Also update onDayClick handler: allow clicking days with bookings even in readOnly mode — pass readOnly down conditionally or add a separate onDayClick for bookings. In the cell click handler: if readOnly && booking exists for this date → call onDayClick?.(dateStr) (so the owner panel can open). Keep the current behavior where readOnly=true prevents click on empty days.

    Update UserCalendarPage.tsx to wire the booking experience:
    - Import useAuth, useState, useAvailableSlots, useBookings, BookingDto
    - Add state: selectedDate (string | null), slotsData (fetched when date selected), myBookings (the user's perspective — but here we need the calendar owner's bookings for display). Use a separate query: bookingApi.getBookingsForOwner — wait, the GET /api/v1/bookings endpoint returns the logged-in user's data. For viewing another user's calendar, we need the booked slots from GET /api/v1/availability/slots which returns availability + booked intervals.
    - Add: const { user } = useAuth(); const isOwnCalendar = user?.id === userId;
    - When viewing another user's calendar (isOwnCalendar === false): side panel should open on day click. Pass mode="booker" to DaySidePanel. Fetch slots when a day is selected using useAvailableSlots(userId, selectedDate). Pass availableSlots and bookedSlots to BookingTimeline via DaySidePanel's new props.
    - Add a side panel state: const [selectedDate, setSelectedDate] = useState<string | null>(null). Pass onDayClick={setSelectedDate} to MonthGrid (MonthGrid currently only fires onDayClick in non-readOnly mode — update to also fire when readOnly but there are booked slots, or add a new allDaysClickable prop — simpler: just set readOnly={false} on MonthGrid here since UserCalendarPage now has its own click handling but no edit capability — instead, add a new prop to MonthGrid onReadOnlyDayClick for this case, or simply handle onDayClick in MonthGrid for readOnly mode when bookings exist).
    - Simplest approach: in UserCalendarPage, pass readOnly={false} to MonthGrid but use a custom onDayClick that sets selectedDate WITHOUT editing. This keeps MonthGrid's click logic working. Pass isSaving={false}, onSave={() => {}} to DaySidePanel as no-ops.
    - Pass ownerId={userId} and ownerEmail={userInfo?.email} to DaySidePanel.

    Also fetch booking data for the MonthGrid display:
    - The slots endpoint GET /api/v1/availability/slots returns day-level data. For MonthGrid, use a monthly bookings summary. Add bookingApi method: getOwnerBookingsForMonth: (ownerId: string, month: string) — but this endpoint doesn't exist in Plan 03-01. Instead, adapt: load bookings block data from the slots response lazily (per day when side panel opens), and use a simpler approach for month grid: add a GET /api/v1/bookings/calendar?ownerId={id}&month={YYYY-MM} endpoint that returns BookingDto[] for display.
    
    REVISED APPROACH to avoid needing a new backend endpoint: In UserCalendarPage, fetch owner bookings using a modified approach. Since GET /api/v1/bookings only returns the logged-in user's bookings, the read-only viewer cannot see another user's bookings detail. For MonthGrid "Unavailable" display, use the already-existing availability calendar data: days with no free slots (after booking subtraction) can be shown as fully booked, but individual booking blocks on the month grid for read-only view are not required by requirements — only the timeline (in side panel) shows booked intervals. The month grid "Unavailable" blocks for read-only view should be derived from the availability calendar response already loaded.

    FINAL APPROACH: 
    - UserCalendarPage month grid: keep readOnly={true} on MonthGrid. Add onDayClick to MonthGrid even in readOnly mode: add optional prop onReadOnlyDayClick?: (date: string) => void. In MonthGrid cell click: if readOnly, call onReadOnlyDayClick?.(dateStr) (no guard on isCurrentMonth). UserCalendarPage passes onReadOnlyDayClick={setSelectedDate}.
    - DaySidePanel in UserCalendarPage: show "booker" mode section. Fetch slots via useAvailableSlots inside UserCalendarPage and pass as props. Display in DaySidePanel via BookingTimeline.
    - For the MonthGrid booking blocks on read-only view: render "Unavailable" blocks only for days where the calendar data shows 0 available ranges (all slots taken). No additional backend call needed.
    - For owner's own MyCalendarPage: pass bookings prop to MonthGrid and DaySidePanel. Fetch bookings via useBookings(). The GET /api/v1/bookings response now includes three lists: incoming (Pending, OwnerId=caller), myBookings (BookerId=caller), and ownedConfirmed (Confirmed, OwnerId=caller). Use BOTH incoming and ownedConfirmed for the owner's MonthGrid — these together cover all D-29 colors: incoming covers amber Pending blocks, ownedConfirmed covers emerald Confirmed blocks.

    Update MyCalendarPage.tsx (minimal change): import useBookings, useAuth, useQueryClient. After existing hooks, add:
    const { data: bookingsData } = useBookings();
    const { user } = useAuth();
    const queryClient = useQueryClient();
    // Combine incoming (Pending) + ownedConfirmed (Confirmed) for full MonthGrid block rendering (D-29)
    const ownerBookings = [
      ...(bookingsData?.incoming ?? []),
      ...(bookingsData?.ownedConfirmed ?? []),
    ];
    Pass bookings={ownerBookings} to MonthGrid. Pass bookings={ownerBookings.filter(b => b.date === selectedDate)} mode="owner" onBookingAction={() => queryClient.invalidateQueries({queryKey:['bookings']})} to DaySidePanel.

    Also update bookingApi.ts (GetBookingsResponse interface) to include the new field: ownedConfirmed: BookingDto[]. And update useBookings.ts hook — no code change needed (the raw response object from the server already includes ownedConfirmed; the TypeScript interface update is sufficient).
  </action>
  <verify>
    <automated>cd client; npx tsc --noEmit 2>&1 | Select-String "error" | Select-Object -First 20</automated>
  </verify>
  <acceptance_criteria>
    - MonthGrid.tsx accepts optional bookings prop and renders amber/emerald blocks for own calendar and zinc "Unavailable" for readOnly
    - MonthGrid.tsx contains "bg-amber-100" and "bg-emerald-100" (booking status colors)
    - MonthGrid.tsx contains "Unavailable" label for readOnly booking blocks
    - DaySidePanel.tsx accepts mode prop ("owner" | "booker" | "read-only") and bookings prop
    - DaySidePanel.tsx contains "Accept Booking" and "Decline Booking" text (owner mode)
    - DaySidePanel.tsx contains "Request Booking" button (booker mode)
    - DaySidePanel.tsx contains BookingConfirmDialog import and render
    - UserCalendarPage.tsx contains useAvailableSlots and selectedDate state
    - UserCalendarPage.tsx contains mode="booker" prop on DaySidePanel
    - MyCalendarPage.tsx contains "ownedConfirmed" (spread into ownerBookings for MonthGrid Confirmed block rendering per D-29)
    - client/src/features/booking/api/bookingApi.ts GetBookingsResponse contains "ownedConfirmed: BookingDto[]"
    - npx tsc --noEmit exits 0
    - Navigate to /users/{id}: click a day → side panel opens with timeline showing available/booked slots and duration picker (visual check)
    - Navigate to /: after accepting a booking, the month grid shows an emerald Confirmed block (not only amber Pending — both colors render correctly per D-29)
    - Navigate to /: click a day that has a pending booking → side panel shows Accept/Decline buttons (visual check, requires a booking in DB)
  </acceptance_criteria>
  <done>Full calendar-integrated booking flow: bookers request from /users/{id} side panel with timeline+dialog; owners approve/decline/cancel from their own calendar side panel; MonthGrid shows color-coded booking blocks. All TypeScript passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BookingTimeline → displayed start times | Only valid start times within free gaps shown — prevents UI from offering times that would conflict |
| DaySidePanel owner mode | Buttons only rendered for bookings where server confirmed caller is the owner (enforced by PATCH endpoints in Plan 03-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Information Disclosure | Booker side panel — booked slots | mitigate | Booked slots in timeline show status label only ("Pending", "Confirmed") — no booker name or email exposed to third-party viewers per UI-SPEC §1 |
| T-03-13 | Information Disclosure | Month grid read-only blocks | mitigate | readOnly mode shows "Unavailable" label only — no status or participant info visible to viewers |
| T-03-14 | Tampering | Client-computed endTime | accept | endTime computed from startTime + selectedDuration client-side is passed to server which validates overlap independently with UPDLOCK; server is the authority |
| T-03-SC | Tampering | No new packages installed in this plan | accept | Only code changes in this plan — no new npm installs required |
</threat_model>

<verification>
After both tasks complete:
1. npx tsc --noEmit exits 0 — no TypeScript errors
2. MonthGrid.tsx contains "bg-amber-100", "bg-emerald-100", "Unavailable"
3. DaySidePanel.tsx contains "Request Booking", "Accept Booking", "Decline Booking"
4. BookingConfirmDialog.tsx contains "Confirm your booking", "Request Booking", "Keep this time"
5. UserCalendarPage.tsx contains "mode=\"booker\""
6. End-to-end test: create a booking via /users/{id} calendar → request appears in /bookings Incoming tab → accept → both users see Confirmed status
</verification>

<success_criteria>
- Booker opens /users/{id}, clicks a day, sees timeline of free/booked slots in side panel
- Booker selects 30min/1hr/2hr/Custom duration and a valid start time; Request Booking button becomes active
- Confirmation dialog shows correct details; submitting fires POST → toast "Request sent — awaiting approval."
- Owner opens / (My Calendar), sees amber Pending blocks and emerald Confirmed blocks on month grid
- Owner clicks day → side panel shows Pending booking with Accept/Decline buttons; Confirmed booking with two-step Cancel
- Read-only calendar (/users/{id}) shows zinc "Unavailable" blocks where slots are taken
- All calendar interactions work without TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/03-booking-engine/03-04-SUMMARY.md` when done
</output>
