---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - src/backend/CalendarBooking.sln
  - src/backend/Domain/Domain.csproj
  - src/backend/Domain/Entities/ApplicationUser.cs
  - src/backend/Domain/Entities/RefreshToken.cs
  - src/backend/Domain/Enums/UserRole.cs
  - src/backend/Application/Application.csproj
  - src/backend/Infrastructure/Infrastructure.csproj
  - src/backend/Infrastructure/Data/ApplicationDbContext.cs
  - src/backend/Infrastructure/Data/Configurations/RefreshTokenConfiguration.cs
  - src/backend/Api/Api.csproj
  - src/backend/Api/Program.cs
  - src/backend/Api/appsettings.json
  - src/backend/Api/appsettings.Development.json
  - client/package.json
  - client/vite.config.ts
  - client/tsconfig.json
  - client/tsconfig.app.json
  - client/index.html
  - client/src/main.tsx
  - client/src/App.tsx
  - client/src/index.css
  - client/src/lib/utils.ts
  - client/components.json
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, ADMIN-01]
user_setup:
  - service: Docker Desktop
    why: SQL Server dev database
    account_setup: "Install Docker Desktop from docker.com — requires WSL2 on Windows"
    dashboard_config:
      - task: "Verify Docker is running after install"
        verification: "docker info"
  - service: SQL Server Docker image
    why: "Database backend for development"
    env_vars:
      - name: MSSQL_SA_PASSWORD
        source: "Set in docker-compose.yml; override via .env for production"

must_haves:
  truths:
    - "Docker SQL Server container starts and accepts connections"
    - "ASP.NET Core API project compiles with no errors"
    - "Vite React frontend compiles with no errors"
    - "shadcn/ui components render correctly"
    - "Frontend dev server proxies /api/* requests to the backend"
    - "Domain entities and DbContext define the correct schema for User and RefreshToken"
  artifacts:
    - path: "docker-compose.yml"
      provides: "SQL Server container definition"
      contains: "mcr.microsoft.com/mssql/server:2022-latest"
    - path: "src/backend/Domain/Entities/ApplicationUser.cs"
      provides: "ApplicationUser entity extending IdentityUser with IsAdmin"
      min_lines: 10
    - path: "src/backend/Infrastructure/Data/ApplicationDbContext.cs"
      provides: "EF Core database context inheriting IdentityDbContext<ApplicationUser>"
      contains: "IdentityDbContext<ApplicationUser>"
    - path: "src/backend/Api/Program.cs"
      provides: "Application entry point with service registration"
      min_lines: 30
    - path: "client/package.json"
      provides: "Frontend dependencies comprising React, shadcn/ui, Tailwind"
      contains: "react"
    - path: "client/src/main.tsx"
      provides: "React app entry point"
      contains: "createRoot"
  key_links:
    - from: "docker-compose.yml"
      to: "SQL Server on port 1433"
      pattern: "1433:1433"
    - from: "client/vite.config.ts"
      to: "backend API at localhost:5000"
      pattern: "proxy.*localhost:5000"
    - from: "src/backend/Api/Program.cs"
      to: "src/backend/Infrastructure/Data/ApplicationDbContext.cs"
      via: "AddDbContext registration"
      pattern: "AddDbContext.*ApplicationDbContext"
    - from: "src/backend/Infrastructure/Data/ApplicationDbContext.cs"
      to: "src/backend/Domain/Entities/ApplicationUser.cs"
      via: "IdentityDbContext<ApplicationUser> generic parameter"
      pattern: "IdentityDbContext<ApplicationUser>"
---

<objective>
Walking Skeleton — scaffold the full project structure with Docker SQL Server, ASP.NET Core 8 Clean Architecture solution, and Vite React 19 frontend with shadcn/ui.

**Purpose:** Establish the development environment and project architecture that all subsequent phases build on. This exercise proves the full stack compiles, connects, and renders.

**Output:** docker-compose.yml, ASP.NET 4-layer Clean Architecture solution, Vite React client with shadcn/ui, EF Core DbContext + domain entities, CORS + JWT middleware skeleton.
</objective>

<execution_context>
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker + ASP.NET Core Backend Solution</name>
  <files>
    docker-compose.yml
    src/backend/CalendarBooking.sln
    src/backend/Domain/Domain.csproj
    src/backend/Application/Application.csproj
    src/backend/Infrastructure/Infrastructure.csproj
    src/backend/Api/Api.csproj
    src/backend/.gitignore
  </files>
  <read_first>
    @.planning/phases/01-foundation-authentication/01-RESEARCH.md (lines 120-138 docker-compose, lines 256-343 project structure, lines 98-108 NuGet packages)
    @.planning/phases/01-foundation-authentication/01-CONTEXT.md (D-11 Clean Architecture, D-13 Docker SQL Server)
  </read_first>
  <action>
    Create the following artifacts at project root:

    1. **docker-compose.yml** — SQL Server 2022 container named `calendar-bookings-db` on port 1433, environment `ACCEPT_EULA=Y`, `MSSQL_SA_PASSWORD=YourStrong!Passw0rd`, volume `sqlserverdata` mounted at `/var/opt/mssql`. SA password is for DEVELOPMENT ONLY — overridable via `.env` for production.

    2. **src/backend/CalendarBooking.sln** — Use `dotnet new sln` to create the solution file, then add 4 projects. Use the `dotnet` CLI for all creation (NOT manual file writes):
       - `dotnet new classlib -n Domain -o src/backend/Domain --framework net8.0` — Pure domain entities, no dependencies.
       - `dotnet new classlib -n Application -o src/backend/Application --framework net8.0` — Add project reference to Domain.
       - `dotnet new classlib -n Infrastructure -o src/backend/Infrastructure --framework net8.0` — Add project references to Domain and Application.
       - `dotnet new webapi -n Api -o src/backend/Api --framework net8.0 --no-https` — Add project references to Application and Infrastructure.
       - Clean the default template files from each project (the auto-generated Class1.cs, WeatherForecast files, etc.).
       - Folder structure per 01-RESEARCH.md project layout.

    3. **NuGet packages** — Add to each project via `dotnet add package`:
       - **Api project**: `Microsoft.AspNetCore.Authentication.JwtBearer 8.0.11`, `Swashbuckle.AspNetCore 6.9.0`, `Serilog.AspNetCore 8.0.3`
       - **Infrastructure project**: `Microsoft.AspNetCore.Identity.EntityFrameworkCore 8.0.11`, `Microsoft.EntityFrameworkCore.SqlServer 8.0.11`, `Microsoft.EntityFrameworkCore.Tools 8.0.11`
       - **Application project**: `FluentValidation.AspNetCore 11.3.0`
       - Api project also needs: `FluentValidation.AspNetCore` (shared registration), `Serilog.Sinks.Console`

    4. **src/backend/.gitignore** — Standard ASP.NET gitignore (bin/, obj/, appsettings.*.local.json, .vs/, *.user, *.suo).

    5. **src/backend/Api/Properties/launchSettings.json** — Configure the Api project to run on `http://localhost:5000` with `ASPNETCORE_ENVIRONMENT=Development`. Use `dotnet run` with `--urls` or profile config; do NOT use IIS Express.

    Per D-11 and research project structure, each .csproj must target `net8.0` (not net10.0) via `<TargetFramework>net8.0</TargetFramework>`.
  </action>
  <verify>
    <automated>Test-Path "src/backend/CalendarBooking.sln"; Get-ChildItem "src/backend" -Recurse -Filter "*.csproj" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml exists with SQL Server 2022 image and port 1433 mapping
    - All 4 .csproj files exist under src/backend/ with net8.0 target
    - Project references are correct (Application→Domain, Infrastructure→Domain+Application, Api→Application+Infrastructure)
    - Api project has JwtBearer, Swashbuckle, Serilog packages installed
    - Infrastructure project has Identity.EntityFrameworkCore, SqlServer packages installed
    - Application project has FluentValidation package installed
    - Api project launches on http://localhost:5000
    - `dotnet build` succeeds at the solution level with zero errors
  </acceptance_criteria>
  <done>
    docker-compose.yml, solution file, and 4 projects compile successfully with `dotnet build`. All NuGet packages resolved without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Domain Entities, DbContext, Program.cs, and App Configuration</name>
  <files>
    src/backend/Domain/Entities/ApplicationUser.cs
    src/backend/Domain/Entities/RefreshToken.cs
    src/backend/Domain/Enums/UserRole.cs
    src/backend/Infrastructure/Data/ApplicationDbContext.cs
    src/backend/Infrastructure/Data/Configurations/RefreshTokenConfiguration.cs
    src/backend/Api/Program.cs
    src/backend/Api/appsettings.json
    src/backend/Api/appsettings.Development.json
  </files>
  <read_first>
    @.planning/phases/01-foundation-authentication/01-RESEARCH.md (lines 743-796 — entity patterns, DbContext pattern, appsettings.json pattern)
    @.planning/phases/01-foundation-authentication/01-CONTEXT.md (D-12 API versioning, D-17 password policy, D-19 Role enum)
  </read_first>
  <action>
    Create the following files with exact content from the research patterns:

    1. **Domain/Entities/ApplicationUser.cs** — Class `ApplicationUser` extends `IdentityUser`. Add properties: `public bool IsAdmin { get; set; } = false;`, `public DateTime CreatedAt { get; set; } = DateTime.UtcNow;`. Per D-19, this is the role model — no IdentityRole table. Add `using Microsoft.AspNetCore.Identity;`.

    2. **Domain/Entities/RefreshToken.cs** — Class with properties: `Id` (Guid, new Guid on create), `Token` (string), `JwtId` (string), `UserId` (string), `User` (ApplicationUser navigation), `IsRevoked` (bool, default false), `CreatedAt` (DateTime), `ExpiresAt` (DateTime). Add `using` for Domain.Entities if needed.

    3. **Domain/Enums/UserRole.cs** — Enum `UserRole` with values `User = 0`, `Admin = 1`. Per D-19, this is for reference only — the actual role check is via `ApplicationUser.IsAdmin` boolean.

    4. **Infrastructure/Data/ApplicationDbContext.cs** — Class inherits `IdentityDbContext<ApplicationUser>`. Constructor takes `DbContextOptions<ApplicationDbContext>`. Expose `DbSet<RefreshToken> RefreshTokens`. Override `OnModelCreating`: call `base.OnModelCreating(builder)` first, then configure RefreshToken entity with HasKey on Id, unique index on Token, one-to-many relationship to User with cascade delete.

    5. **Infrastructure/Data/Configurations/RefreshTokenConfiguration.cs** — IEntityTypeConfiguration<RefreshToken> with the same configuration as step 4 (cleaner separation). Apply in ApplicationDbContext.OnModelCreating via `builder.ApplyConfiguration(new RefreshTokenConfiguration())`.

    6. **Api/Program.cs** — Minimal API setup with:
       - Serilog bootstrap logger (`.CreateBuilder()` pattern with WriteTo.Console)
       - `AddDbContext<ApplicationDbContext>` with `UseSqlServer` from connection string "DefaultConnection"
       - `AddIdentity<ApplicationUser, IdentityRole>` with password options: RequiredLength=8, RequireDigit=true, RequireLowercase=true, RequireUppercase=true, RequireNonAlphanumeric=true (per D-17). Then `.AddEntityFrameworkStores<ApplicationDbContext>().AddDefaultTokenProviders()`
       - `AddAuthentication(JwtBearerDefaults.AuthenticationScheme).AddJwtBearer(...)` with token validation parameters and `OnMessageReceived` reading from `Request.Cookies["AccessToken"]` — this is the **skeleton config**; Jwt Key/Issuer/Audience values come from appsettings.json
       - `AddAuthorization()`
       - `AddCors` with dev policy: `WithOrigins("http://localhost:5173").AllowCredentials().AllowAnyHeader().AllowAnyMethod()` — per Research Pitfall 1, CORS must allow credentials for httpOnly cookies. SameSite=Lax for development.
       - `AddControllers()` + Swagger/OpenAPI config (Swagger only in Development)
       - Middleware pipeline: `UseCors("DevCors")` → `UseAuthentication()` → `UseAuthorization()` → `MapControllers()`
       - Keep runtime seeder call commented out (Plan 02 wires it)
       - `app.Run()` at end
       - Add using directives for all required namespaces

    7. **Api/appsettings.json** — JWT section with placeholder values:
       ```json
       { "Jwt": { "Key": "SuperSecretKeyThatIsAtLeast32CharactersLong", "Issuer": "CalendarBookingApi", "Audience": "CalendarBookingApp", "AccessTokenExpirationMinutes": 15, "RefreshTokenExpirationDays": 7 }, "AdminSeed": { "Email": "admin@calbooking.com", "Password": "Admin123!", "IsAdmin": true } }
       ```
       ConnectionStrings section with `DefaultConnection` pointing to `Server=localhost,1433;Database=CalendarBookingDb;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;`.
       The `AdminSeed` section is per D-07/D-09 — admin credentials configurable from appsettings.

    8. **Api/appsettings.Development.json** — Override `AdminSeed.Password` with same dev value for explicit clarity. Set `Logging` to Verbose.
  </action>
  <verify>
    <automated>dotnet build src/backend/CalendarBooking.sln 2>&1 | Select-String -Pattern "error" -NotMatch</automated>
  </verify>
  <acceptance_criteria>
    - ApplicationUser extends IdentityUser with IsAdmin + CreatedAt properties
    - RefreshToken entity has Token, JwtId, UserId, IsRevoked, CreatedAt, ExpiresAt
    - ApplicationDbContext inherits IdentityDbContext<ApplicationUser> with RefreshTokens DbSet
    - RefreshToken has unique index on Token, FK to ApplicationUser with cascade delete
    - Program.cs registers Identity, JwtBearer (cookie reader), CORS (localhost:5173), Swagger, Controllers
    - Program.cs JwtBearer OnMessageReceived reads AccessToken from cookie
    - appsettings.json has Jwt section with 32+ char Key, Issuer, Audience, expiration values
    - appsettings.json has ConnectionStrings.DefaultConnection for SQL Server at localhost:1433
    - appsettings.json has AdminSeed section with email, password, IsAdmin
    - `dotnet build` succeeds with zero errors
  </acceptance_criteria>
  <done>
    All domain entities, DbContext, and configuration files compile. The API project builds successfully with Identity + JwtBearer middleware configured.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Vite React Frontend with shadcn/ui</name>
  <files>
    client/package.json
    client/vite.config.ts
    client/tsconfig.json
    client/tsconfig.app.json
    client/index.html
    client/src/main.tsx
    client/src/App.tsx
    client/src/index.css
    client/src/lib/utils.ts
    client/components.json
  </files>
  <read_first>
    @.planning/phases/01-foundation-authentication/01-RESEARCH.md (lines 109-118 npm install commands, lines 539-600 axios pattern — for proxy config)
    @.planning/phases/01-foundation-authentication/01-UI-SPEC.md (entire file — design system, page specs)
    @.planning/phases/01-foundation-authentication/01-CONTEXT.md (D-10 frontend stack decision)
  </read_first>
  <action>
    Create the frontend project using Vite + React + TypeScript:

    1. **Scaffold with Vite CLI:**
       ```powershell
       npm create vite@latest client -- --template react-ts
       ```
       This creates the `client/` directory with package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, vite.config.ts, src/main.tsx, src/App.tsx, src/index.css, and other starter files.

    2. **Install dependencies:**
       ```powershell
       cd client
       npm install react-router-dom axios @tanstack/react-query
       npm install react-hook-form @hookform/resolvers zod
       npm install tailwindcss @tailwindcss/vite @types/node
       npx shadcn@latest init -y --style new-york --base-color zinc
       npx shadcn@latest add button card input label form sonner
       ```
       - Use `--style new-york --base-color zinc` for shadcn init (per UI-SPEC design system). Select CSS variables for theme.
       - The `@tailwindcss/vite` plugin is for Tailwind v4, which is the shadcn v4 standard.
       - For shadcn init, select the following when prompted: New York (style), Zinc (base color), CSS variables (yes), `@tailwindcss/vite` (import path).

    3. **Configure Vite proxy:**
       In `vite.config.ts`, add a proxy rule so `/api` requests forward to the ASP.NET backend:
       ```typescript
       server: {
         proxy: {
           '/api': {
             target: 'http://localhost:5000',
             changeOrigin: true,
           },
         },
       },
       ```
       This solves the cross-origin cookie issue (Research Pitfall 1) by making all API requests same-origin in development. The `withCredentials: true` Axios setting works because the Vite dev server proxies the request.

    4. **Set up CSS entry:**
       In `src/index.css`, replace the template content with:
       ```css
       @import "tailwindcss";
       @import "tw-animate-css";
       ```
       (shadcn init creates the `tw-animate-css` import automatically for the CSS variables theme.)

    5. **Configure tsconfig path alias:**
       In `tsconfig.app.json`, add path mappings for `@/*` → `./src/*`:
       ```json
       { "compilerOptions": { "baseUrl": ".", "paths": { "@/*": ["./src/*"] } } }
       ```
       Update `vite.config.ts` with:
       ```typescript
       import path from "path"
       resolve: { alias: { "@": path.resolve(__dirname, "./src") } }
       ```

    6. **Create utility file:**
       `src/lib/utils.ts` — the shadcn `cn()` helper:
       ```typescript
       import { type ClassValue, clsx } from "clsx"
       import { twMerge } from "tailwind-merge"
       export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
       ```
       (shadcn init creates this automatically — verify it exists.)

    7. **Create minimal App shell:**
       `src/App.tsx` — placeholder with React Router setup:
       ```tsx
       import { BrowserRouter, Routes, Route } from 'react-router-dom'
       function App() {
         return (
           <BrowserRouter>
             <Routes>
               <Route path="/" element={<div>Calendar Booking System</div>} />
             </Routes>
           </BrowserRouter>
         )
       }
       export default App
       ```

    8. **Verify build:**
       ```powershell
       cd client && npm run build
       ```

    IMPORTANT: Do NOT mutate the auto-generated `vite-env.d.ts` or `public/` files. The `tsconfig.node.json` is for Vite's own config resolution — do not touch it.
  </action>
  <verify>
    <automated>cd client; npm run build 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `client/package.json` exists with all required dependencies (react-router-dom, axios, @tanstack/react-query, react-hook-form, zod, tailwindcss, @tailwindcss/vite)
    - shadcn/ui initialized with New York style, Zinc base color
    - shadcn components added: button, card, input, label, form, sonner
    - Vite dev server start succeeds (`npm run dev` starts without errors)
    - `npm run build` completes with zero errors
    - Vite proxy routes `/api/*` to `http://localhost:5000`
    - Path alias `@/` resolves to `./src/*`
    - `src/lib/utils.ts` exports `cn()` helper function
    - `src/App.tsx` renders a basic route structure
  </acceptance_criteria>
  <done>
    Vite React frontend compiles and serves correctly. shadcn/ui components are installed and importable. Vite proxy forwards API requests to the ASP.NET backend. Full-stack `dotnet build` + `npm run build` both succeed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker host → SQL Server container | SQL Server exposed on host port 1433 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | NuGet/npm package installs | mitigate | All packages from official registries (nuget.org, npmjs.com); verified in RESEARCH.md Package Legitimacy Audit (all [ASSUMED]) |
| T-01-02 | Information Disclosure | SQL Server port 1433 exposed | accept | Dev-only; docker-compose port binding not exposed in production |
| T-01-03 | Spoofing | CORS policy allows localhost:5173 | accept | Dev-only; restrict origins in production deployment |
</threat_model>

<verification>
- [ ] `docker-compose up` starts SQL Server container and accepts connections
- [ ] `dotnet build` at solution level succeeds with zero warnings-as-errors
- [ ] `npm run build` in client/ completes with zero errors
- [ ] `npm run dev` starts Vite dev server on port 5173
- [ ] shadcn button component renders in App.tsx test
</verification>

<success_criteria>
- Full project structure exists: docker-compose.yml → ASP.NET 4-layer Clean Architecture → Vite React client
- Both frontend and backend build without errors
- SQL Server container starts and listens on port 1433
- Vite dev server proxies /api/* to ASP.NET on port 5000
- shadcn/ui components installed and importable
- Domain entities (ApplicationUser, RefreshToken) defined with correct properties
- EF Core DbContext configured with Identity support
- Program.cs has all middleware wired (Identity, JwtBearer, CORS, Swagger, Controllers)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md` when done
</output>
